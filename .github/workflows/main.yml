# This is a test workflow
name: CI
on:
  push:
    branches: main
    paths: 'package.json'
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Read package.json version
        run: grep version ${{ github.workspace }}/package.json | awk -F \" '{print $4}'
      - name: List releases
        run: gh release list 
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Create new Github release
        run: gh release create v1.0.23 --generate-notes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # - name: Extract version from package.json
      #   run: |
      #     PACKAGE_VERSION=awk '/version/{gsub(/("|",)/,"",$2);print $2}' ${{ github.workspace }}/package.json
      # - run: echo "This is version no ${{ PACKAGE_VERSION }}."
      - run: echo "This job's status is ${{ job.status }}."